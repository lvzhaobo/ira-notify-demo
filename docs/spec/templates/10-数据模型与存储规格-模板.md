# 10 — 数据模型与存储规格（模板）

> **提示**：表名、主键、索引与 `09` DTO 字段对齐；迁移文件放 `backend/migrations/`。定稿后合并到 **`spec/10-数据模型与存储规格.md`**。

## 表清单（示例）

| 表名 | 用途 |
|------|------|
| notify_channel_configs | 渠道配置 |
| notify_rules | 推送规则 |
| notify_deliveries | 投递记录 |

## 字段草案

### notify_rules（示例）

| 列 | 类型 | 说明 |
|----|------|------|
| id | TEXT PK | UUID |
| name | TEXT |  |
| enabled | INTEGER | 0/1 |

（继续补充）
