# 复制为 .env 后按需填写（勿提交 .env）
FLASK_ENV=development
SECRET_KEY=change-me-in-workshop
# 可选：SQLite 文件路径（Docker compose 的 api 服务默认使用 /data/notify.db）
# M4_DATABASE_PATH=/absolute/path/to/notify.db
# 可选：后续换 PostgreSQL 时启用
# DATABASE_URL=postgresql://user:pass@localhost:5432/m4_notify
