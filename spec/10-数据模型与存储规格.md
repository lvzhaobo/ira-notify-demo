# 10 — 数据模型与存储规格（工作稿）

> **工作坊提示**：`backend/migrations/001_init.sql` 当前 **无有效 CREATE**（仅注释）。表结构以本文件定稿为准，再写迁移。讨论用模板：`docs/spec/templates/10-数据模型与存储规格-模板.md`。

## 存储

- 开发：SQLite 文件（`notify.db`）即可。
- 提示：合并前与 B 角色确认索引与查询维度（列表、过滤）。

## 表（TODO）

> 提示：至少覆盖 **渠道配置、规则、投递记录**；字段与 `09` DTO 对齐。

| 表名 | 用途 | 状态 |
|------|------|------|
|  |  | 待填写 |

## 迁移策略

1. 在 `migrations/` 增加编号 SQL 或 Alembic（若引入）。
2. `db.init_db` 执行迁移的顺序写进 README。
