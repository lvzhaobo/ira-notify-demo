# 05 — 用户故事与验收标准（工作稿）

> **工作坊提示**：以下为 **骨架句**；请小组在 PR 中补全 AC，并与 `09` 行为一致。讨论用模板：`docs/spec/templates/05-用户故事与验收标准-模板.md`。

| 属性 | 内容 |
|------|------|
| 模块 | M4 多渠道推送 |
| 版本 | workshop-draft |

---

## US-M4-001（P0）规则 CRUD

| 字段 | 内容 |
|------|------|
| **故事** | 作为运营，我希望增删改推送规则并绑定渠道，以便管理投放策略。 |
| **AC** | 1）`POST/GET/PATCH/DELETE /notify/rules` 与 `09` 一致；2）非法 `channelIds` → **400**；3）（可选）禁用规则 `dispatch` → **409** `M4_RULE_DISABLED`。 |
| **REQ** | REQ-M4-001 |

> 提示：实现前请冻结 `09` 中 rules 相关 DTO 与错误码。

---

## US-M4-002（P0）试发与历史

| 字段 | 内容 |
|------|------|
| **故事** | 作为运营，我希望试发并查看投递历史，以便确认触达与审计。 |
| **AC** | 1）`POST /notify/dispatch` 默认 `dryRun=true` 时不调用外网（或仅用沙箱）；2）`GET /notify/deliveries` 含 `traceId`、`status`；3）`dryRun=false` 时有可核验回执。 |
| **REQ** | REQ-M4-002 |

> 提示：`dryRun` 默认值务必在 `09` 与后端实现中一致。

---

## US-M4-003（P0）渠道测试

| 字段 | 内容 |
|------|------|
| **故事** | 作为运营，我希望测试渠道连通性，以便上线前校验配置。 |
| **AC** | `POST /notify/channels/{type}/test` 返回 `ok` 或明确错误；**不得**在响应中返回完整 webhook 密钥。 |
| **REQ** | REQ-M4-003 |

> 提示：密钥用 `secret_ref` 类字段；日志与响应需脱敏。
